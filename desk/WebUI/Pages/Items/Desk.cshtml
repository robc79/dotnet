@page
@using Desk.WebUI.ViewModels
@model Desk.WebUI.Pages.Items.DeskModel
@{
    ViewData["Title"] = "Desk";
}

<div class="text-center">
    <h1 class="display-4">Desk</h1>
</div>

<div class="row justify-content-center mb-3">
    <div class="col-8">
        Here are your in progress projects.
        
        @foreach (var item in Model.DeskItems)
        {
            <partial name="_ItemSummaryPartial" model="new ItemSummaryViewModel(item, false)" />
        }
    </div>
</div>
